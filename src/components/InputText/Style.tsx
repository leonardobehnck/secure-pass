import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    fontSize: 18,
    color: "#000",
    textAlign: "center",
    borderRadius: 10,
    backgroundColor: "#e8e8e8",
    padding: 20,
  },
});
